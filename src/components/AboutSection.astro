---
import { contactInfo } from '@lib/data';
---

<main class='grid md:place-items-center md:mt-0 mt-14 md:h-screen w-full'>
  <div class='max-w-4xl px-5 flex flex-col w-full items-center gap-5'>
    <div
      class='flex gap-3 flex-col-reverse md:flex-row w-full justify-between items-center'
    >
      <div class='flex flex-col gap-3'>
        <h1 class='font-bold text-4xl'>Omar Obregon</h1>
        <h2 class='text-xl text-secondary'>Fullstack Developer</h2>

        <div class='flex flex-col'>
          {
            contactInfo.map((contact) => (
              <a
                class='text-secondary flex items-center gap-2 text-sm'
                href={contact.url}
                rel='noopener noreferrer'
                target='_blank'
              >
                <span class={contact.icon} role='img' aria-hidden='true' />
                <span>{contact.name}</span>
              </a>
            ))
          }
        </div>
      </div>

      <div>
        <img
          class='rounded-full size-[250px] md:size-[300px]'
          src='/photo.png'
          alt='Photo'
        />
      </div>
    </div>

    <div
      class='bg-gradient-to-r from-purple-500/10 to-pink-500/10 backdrop-blur-sm backdrop-filter p-4 rounded-lg border border-border shadow-sm'
    >
      <p class='text-muted-foreground pl-4 text-secondary py-2 relative'>
        <span
          class='absolute left-0 top-0 h-full w-1 bg-gradient-to-b from-purple-500 to-pink-500 rounded-full'
        ></span>
        🚀 I am a passionate full-stack web developer dedicated to building modern,
        user-friendly applications. I enjoy turning ideas into clean, scalable, and
        efficient solutions, always paying attention to detail and thoughtful design.
        With a focus on both functionality and aesthetics, I strive to create digital
        experiences that are not only reliable but also engaging.
      </p>
    </div>
  </div>
</main>
